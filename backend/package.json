{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production --minify",
    "migrate:local": "wrangler d1 migrations apply wellness-hub-db --local",
    "migrate:prod": "wrangler d1 migrations apply wellness-hub-db --env production --remote",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "node --test test/**/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.8",
    "otplib": "^13.3.0",
    "uuid": "^13.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260313.1",
    "drizzle-zod": "^0.5.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.4.0"
  }
}
